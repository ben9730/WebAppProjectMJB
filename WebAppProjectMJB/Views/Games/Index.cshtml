@model IEnumerable<WebAppProjectMJB.Models.Game>

@{
    ViewData["Title"] = "Index";
}

<h3 style="font-size:30px; color:white";>Ps4 Game List</h3>
<body style="background: url(https://i.pcmag.com/imagery/reviews/01aNaoc1M3kOtwVWPTMD9ms-13.1613593352.fit_scale.size_760x427.jpg) no-repeat center center fixed;
        background-size: 100% 100%;">
    </body>

<p>
    <a asp-action="Create">Create New</a>
</p>

@*the search tab in the page, name is for the funtion in the gameControler
    asp-action is the name of the func in the controler*@

<div class="container">
    <form asp-action="SearchPs" class="col-sm-3 form-group">
        <input class="form-control" name="query" type="search" placeholder="type game to search">
        <br />
        <input type="submit" class="btn btn-primary" value="Search" />
    </form>
</div>

<div class="container">
    <div class="row">
        @foreach (var game in Model) //the main loop for put more items
        {
            @if (game.Console.Name == "Ps4")
            {
                <div class="col-sm-3">

                    <div class="card" style="width: 18rem;">
                        <img src="@game.CoverImage" class="card-img-top" alt="no">
                        <div class="card-body">
                            <h5 class="card-title"> @game.Name</h5>
                            <p class="card-text">Console: @game.Console.Name</p>
                            <p class="card-text"> Price:  @game.Price₪</p>
                            <a asp-action="Details" asp-route-id="@game.Id" class="btn btn-primary">Go to Game page</a>
                            <div>
                                <a asp-action="Edit" asp-route-id="@game.Id">Edit</a> |
                                <a asp-action="Delete" asp-route-id="@game.Id">Delete</a>
                            </div>
                        </div>
                    </div>


                </div>
            }
        }

    </div>
</div>